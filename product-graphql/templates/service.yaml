apiVersion: v1
kind: Service
metadata:
  name: {{ include "product-graphql.fullname" . }}
  labels:
    {{- include "product-graphql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "product-graphql.selectorLabels" . | nindent 4 }}
